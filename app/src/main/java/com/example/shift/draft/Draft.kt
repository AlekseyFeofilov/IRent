package com.example.shift.draft

class MainActivity{
    /*
        var debug: String? = null
        IrentApp.retrofit
            .create(AuthorizationApi::class.java)
            .checkBackend()
            .enqueue(object : Callback<ResponseBody> {
                override fun onResponse(
                    call: Call<ResponseBody>,
                    response: Response<ResponseBody>
                ) {
                    debug = response.body().toString()
                }

                override fun onFailure(call: Call<ResponseBody>, t: Throwable) {
                    Log.e("Backend", t.toString())
                }
            })
*/

    /*val stream = ByteArrayOutputStream()
    binding.testImageView.drawToBitmap(Bitmap.Config.ARGB_8888).compress(Bitmap.CompressFormat.JPEG, 80, stream)
    val byteArray = stream.toByteArray()
    val multipart = MultipartBody.Part.createFormData(
        "sf", "sdf", byteArray.toRe
    )*/

    /*IrentApp.retrofit
        .create(AuthorizationApi::class.java)
        .addIcon()
        .enqueue(object : Callback<String> {
            override fun onResponse(call: Call<String>, response: Response<String>) {
                debug = response.body()
            }

            override fun onFailure(call: Call<String>, t: Throwable) {
                Log.e("Backend", t.toString())
            }
        })*/
}